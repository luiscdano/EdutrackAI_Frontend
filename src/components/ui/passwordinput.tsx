import Input from "./input";

const PasswordInput = () => {
  return (
    <Input
      label="Contraseña"
      type="password"
      placeholder="Ingrese su contraseña"
    />
  );
};

export default PasswordInput;